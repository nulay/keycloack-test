FROM tomcat:8.5-alpine

LABEL maintainer="deepak@softwareyoga.com"

ADD https://github.com/nulay/keycloack-test/blob/inner-registration/spring-boot-with-keycloak/war/keycloak1.war?raw=true /usr/local/tomcat/webapps/

EXPOSE 8080

CMD ["catalina.sh", "run"]